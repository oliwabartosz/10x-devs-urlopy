---
import Layout from "@/layouts/Layout.astro";

const isAutoConfirmed = import.meta.env.DEV;

const content = isAutoConfirmed
  ? {
      emoji: "✅",
      heading: "Registration successful",
      description: "Your account has been created. You can now sign in.",
      linkText: "Go to sign in",
    }
  : {
      emoji: "📧",
      heading: "Check your email",
      description: "We've sent a confirmation link to your email address. Click it to activate your account.",
      linkText: "Back to sign in",
    };
---

<Layout title={content.heading}>
  <div class="bg-surface flex min-h-screen items-center justify-center p-4">
    <div class="w-full max-w-md">
      <div class="border-line overflow-hidden rounded-2xl border bg-white p-8 text-center shadow-xl shadow-slate-200">
        <div class="mb-4 text-5xl">{content.emoji}</div>
        <h1 class="text-primary mb-3 text-2xl font-bold">{content.heading}</h1>
        <p class="text-muted-foreground mb-6">{content.description}</p>
        <a href="/" class="text-primary hover:text-accent text-sm font-semibold transition-colors">
          {content.linkText}
        </a>
      </div>
    </div>
  </div>
</Layout>
